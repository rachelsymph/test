const routes = {
  DASHBOARD: '/dashboard',
  GIVES: '/admin/gives',
  LOGIN: '/login',
  PLATFORMS: '/admin/platforms',
  RECIPIENTS: '/admin/recipients',
  REGEXES: '/admin/regexes',
  REGISTER: '/register',
  ROOT: '/',
};

export default routes;
